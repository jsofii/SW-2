import { FiltrolaboPipe } from './filtrolabo.pipe';

describe('FiltrolaboPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrolaboPipe();
    expect(pipe).toBeTruthy();
  });
});
