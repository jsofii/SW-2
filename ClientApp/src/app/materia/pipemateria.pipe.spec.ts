import { PipemateriaPipe } from './pipemateria.pipe';

describe('PipemateriaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipemateriaPipe();
    expect(pipe).toBeTruthy();
  });
});
